{
  "kind": "singleType",
  "collectionName": "statuses",
  "info": {
    "singularName": "status",
    "pluralName": "statuses",
    "displayName": "Status"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "Status": {
      "type": "enumeration",
      "enum": [
        "Currently Unavailable",
        "Currently Available"
      ],
      "default": "Currently Available",
      "required": true
    }
  }
}
